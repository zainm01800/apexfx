"""APEX Quant Engine - a forex-first quantitative signal & risk service.

Philosophy (encoded throughout, not just documented):
  * The edge is in risk management, regime awareness, and validation discipline
    - NOT prediction. Treat every single signal as weak.
  * Outputs are probabilistic with explicit uncertainty, never certainties.
  * The risk layer has veto authority over every signal.
  * Overfitting is the default failure mode; every design choice fights it.
  * Forex-first, architected for multi-asset extension later.
"""

__version__ = "0.1.0"
