# Full Book R stop-overlay audit JSON

Concatenate `chunk-000` through `chunk-003` in filename order to recover the
deterministic gzip stream for `book_r_stop_overlay_audit_2026-09-03.json`.

- Combined gzip bytes: 51,205
- Combined gzip SHA-256:
  `c42890b697ae59adc58184e304e8762803902d059765a41988ae7de0f2b4bb56`

The readable Markdown report is stored beside this directory. The chunks keep
the complete machine-readable event ledger reproducible despite the repository
host's broken handling of larger Git objects.
