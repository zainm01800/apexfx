# APEX Quant Engine — Phase 1 dependencies
# Pinned to versions with prebuilt wheels for Python 3.12 on Windows.
# After a clean install, freeze exact versions: pip freeze > requirements.lock.txt

# ── API layer ────────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.1

# ── Data / maths ─────────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
exchange-calendars>=4.5,<5  # authoritative XNYS sessions for Book R month-end decisions
scipy>=1.11
pyarrow>=14          # parquet historical store
httpx>=0.26          # async OHLCV fetching + DeepSeek API calls
yfinance>=0.2.65     # frozen Book G research snapshot downloader
pyyaml>=6            # versioned config

# ── Models ───────────────────────────────────────────────────────────────────
scikit-learn>=1.3
statsmodels>=0.14
arch>=6.2            # GARCH-family volatility (used by GARCHForecast feature)
hmmlearn>=0.3.0      # HMM regime detection
lightgbm>=4.1        # gradient-boosting ensemble (Phase 2 signal expansion)

# ── Execution ─────────────────────────────────────────────────────────────────
pyzmq>=25            # ZeroMQ TCP push-pull bridge (optional; falls back to file bridge)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0
